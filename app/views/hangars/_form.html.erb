<%= form_for(@hangar) do |f| %>
  <% if @hangar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hangar.errors.count, "error") %> prohibited this hangar from being saved:</h2>

      <ul>
      <% @hangar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <strong>Select Airport</strong>
 <%= f.collection_select :airport_id, Airport.all, :id, :formatted_name %></br><br>
  <div class="field">
    <strong>Hangar Number or Tie Down ID: </strong>    <%= f.text_field :hangar_number %>
  </div>
<strong>Hangar Style</strong>
  <div class="field"> <%= f.select :hangar_type, ['Open / Shade', 'Enclosed T Hangar', 'Standard', 'Shared', 'Outdoor Tiedown', 'Other'], :prompt => 'Select One' %>
  </div>
  <div class="field">
    <%= f.label :size %> <%= f.text_field :size %>
  </div>
    <div class="field">
    Monthly Rate:  $<%= f.number_field :rate %>
  </div>
    <div class="field">
    Minimum Lease Term: <%= f.number_field :term %> <i>(in months)</i>
  </div>
<center>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
